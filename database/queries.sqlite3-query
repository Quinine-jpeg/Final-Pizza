-- database: ../database/data.db

--drop table if exists users;
--create table users (id integer primary key autoincrement, username text not null, password text not null, email text not null, address text not null, credit integer not null);
--drop table drivers

--drop table if exists orders;
--create table orders (id integer primary key autoincrement, userid integer, pizzas text not null, address text, status text not null, price integer not null);
-- reset id num at a certain time each day using lines above

--drop table if exists pizzas;
--create table pizzas (id integer primary key autoincrement, name text not null, price integer not null, descrip text, img text);
-- keep images at like 100x100

--insert into orders (userid, pizzas, address, status) values (1, '[5, 4, 7]', '10 Downing Street', 'pending')
--insert into orders (userid, pizzas, address, status) values (2, '[2, 1, 8, 11]', '331b Baker Street', 'pending')
--insert into orders (userid, pizzas, address, status) values (3, '[8, 3]', 'Middle of Nowhere', 'pending')
